Instructions to run your scripts before starting Liferay:
* Write some script, or use the provided ones (demo). Copy them in `scripts/` folder
* Bind-mount the parent directory into the container under `/mnt/liferay/` 
